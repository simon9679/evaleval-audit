# POSTRUN_METHOD_NOTE — T11

Status: post-run meta-audit note.

## Method behavior observed

T11 changed the interpretation of the mixed-unit branch.

A unit-only framing is now insufficient because the same consequential groups
also differ in source metric identity and score bounds.

One group additionally differs in metric family and direction.

## Protocol contribution

Rule 2 supported decomposition of the source-definition variation.

Rule 6 now requires following those source identities through
canonicalization.

Rule 8 kept the field set fixed before inspection.

## Handbook contribution

B12 is especially informative here: a comparability grouping rule is
claim-governing when it decides which rows are treated as the same
measurement.

U8 prevents T11 from jumping directly to semantic invalidity.

U11 requires the next attribution step to identify the transformation boundary.

## Candidate methodological lesson

When a downstream grouping appears to combine heterogeneous rows, compare the
upstream source identity with the downstream canonical identity explicitly.

A downstream single canonical id does not prove source homogeneity.
